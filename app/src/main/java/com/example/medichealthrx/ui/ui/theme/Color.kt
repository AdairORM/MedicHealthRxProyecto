package com.example.medichealthrx.ui.ui.theme

import androidx.compose.ui.graphics.Color

// Colores para el tema claro
val LightBackground = Color(0xFFFFFFFF) // Fondo blanco
val LightOnBackground = Color(0xFF000000) // Texto negro
val LightPrimary = Color(0xFF2196F3) // Azul claro primario
val LightOnPrimary = Color(0xFFFFFFFF) // Texto en botones primarios (blanco)
val LightSecondary = Color(0xFF64B5F6) // Azul secundario
val LightOnSecondary = Color(0xFFFFFFFF) // Texto en botones secundarios (blanco)

// Colores para el tema oscuro
val DarkBackground = Color(0xFF000000) // Fondo negro
val DarkOnBackground = Color(0xFFFFFFFF) // Texto blanco
val DarkPrimary = Color(0xFF82B1FF) // Azul claro en modo oscuro
val DarkOnPrimary = Color(0xFF000000) // Texto en botones primarios (negro)
val DarkSecondary = Color(0xFF4FC3F7) // Azul secundario oscuro
val DarkOnSecondary = Color(0xFF000000) // Texto en botones secundarios (negro)

// Colores comunes
val ErrorRed = Color(0xFFD32F2F) // Rojo para errores
val OnError = Color(0xFFFFFFFF) // Texto blanco en errores
